<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="CustomedtabRow" >
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:background">"@color/abc_input_method_navigation_guard</item>
        <item name="android:layout_margin">@dimen/table_row_margin</item>
        <item name="android:paddingLeft">@dimen/activity_horizontal_margin</item>
        <item name="android:paddingRight">@dimen/activity_horizontal_margin</item>
    </style>
    <style name="CustomedButton" parent="Base.TextAppearance.AppCompat.Widget.Button">
        <item name="android:layout_gravity">center_horizontal</item>
        <item name="android:gravity">center</item>
        <item name="android:layout_column">0</item>
        <item name="android:background">@color/abc_input_method_navigation_guard</item>
        <item name="android:textColor">@color/bright_foreground_material_dark</item>
        <item name="android:onClick">sendMessage</item>
    </style>
</resources>
